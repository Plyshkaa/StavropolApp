<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo_app"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_app"
        android:supportsRtl="true"
        android:theme="@style/Theme.StavropolPlacesApp"
        tools:targetApi="31">

        <activity
            android:name=".SplashScreenActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".PlaceDetailActivity"
            android:exported="true"
            android:label="Place Detail" />

        <activity android:name=".PlacesActivity" />
        <activity android:name=".ZemlyakiActivity" />
        <activity android:name=".PersonDetailActivity" /> <!-- Добавляем PersonDetailActivity -->
        <activity android:name=".RegionDetailActivity" />
        <activity android:name=".AfishaActivity" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <activity
            android:name=".PlacesListActivity"
            android:exported="true"
            android:label="Places" />
    </application>

</manifest>
